@model IEnumerable<CreativeCollabMusicalRecipes.Models.InstructorDto>
@{
    ViewBag.Title = "Instructor List";
}
@*listing intructor information*@
<h2 class="text-center">Instructor List</h2>
<div class="row  d-flex justify-content-center align-content-center">
    @foreach (var instructor in Model)
    {
        <div class="col-md-4">
            <div class="card mb-4">
                <div class="card-body">
                    <h5 class="card-title"><strong>Instructor Name:</strong> @instructor.FirstName @instructor.LastName</h5>
                    <p class="card-text"><strong>Instructor Number:</strong> @instructor.InstructorNumber</p>
                    @*<p class="card-text"><strong>Hire Date:</strong> @instructor.HireDate.ToShortDateString()</p>
                    <p class="card-text"><strong>Wages:</strong> @instructor.Wages</p>
                    <p class="card-text"><strong>Academy ID:</strong> @instructor.AcademyId </p>*@
                    <a href="/Instructor/Details/@instructor.InstructorId" class="btn btn-primary">View Details</a>

                </div>
            </div>
        </div>
    }
    <a href="/Instructor/New" class="btn btn-secondary mt-3">Add Instructor</a>
</div>
